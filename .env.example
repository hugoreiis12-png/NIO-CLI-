# Database connection string 
NIO_DATABASE_URL=postgres://user:senha@localhost:5432/nio_cli

# TLS — descomente e ponha "true" só se o banco exigir (gerenciado/nuvem).
# NIO_DATABASE_SSL=true

# Gateway — JWT (login v2)
JWT_SECRET=
# JWT_EXPIRES_IN=12h
